services:
  api:
    build:
      context: ./backend
      dockerfile: api/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - keepalive_data:/app/data
    environment:
      DATABASE_URL: sqlite:///app/data/keepalive.db?mode=rwc

volumes:
  keepalive_data:
