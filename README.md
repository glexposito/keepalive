# keepalive

`keepalive` is a self-hosted status page for your projects.

It is a lean work-in-progress tool in the same general space as Uptime Kuma.

## Run

From the project root:

```bash
docker compose up
```

Then open `http://localhost:3000/docs`.

## Status

Work in progress.
